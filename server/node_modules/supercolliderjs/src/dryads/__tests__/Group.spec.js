/* @flow */
import Group from '../Group';
// import { DryadicPlayer } from 'dryadic';
import _ from 'lodash';
// import { expectPlayGraphToEqual } from '../utils/test-utils';

describe('Group', function() {
  // a Group's child context should have .group set to the Group's nodeID
  describe('prepareForAdd set context', function() {
    let f = new Group();
    let g = new Group({}, [f]);

    it('should make command object', function() {
      // let p = new DryadicPlayer(g);
      let c = g.prepareForAdd();
      expect(_.isPlainObject(c)).toBe(true);
    });

    //  but this will boot the server
    // it('a Group\'s child context should have .group set to the Group\'s nodeID', function() {
    //   let p = new DryadicPlayer(g);
    //   return p.prepare().then(() => {
    //     let nodeID =
    //     expect(s);
    //   });
    // });
  });
});
